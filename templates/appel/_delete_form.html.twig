<form method="post" action="{{ path('app_appel_delete', {'id': appel.id}) }}" onsubmit="return confirm('Etes vous sûr de supprimer cette appel ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appel.id) }}">
    <button class="btn btn-danger"><img width="20px" class="delete" src="{{asset('./images/icon-delete.png')}}"/></button>
</form>
