<form method="post" action="{{ path('client_show_all')}}" id="recherche-client">
        <div class="input-group">
                <div class="form-outline">
                        <input class="form-control me-2" name='recherche' type="search" placeholder="Recherhcer" aria-label="Search">
                </div>
                <button type="submit" class="btn btn-primary">
                       <i class="bi bi-search"></i>
                </button>
        </div>
</form>