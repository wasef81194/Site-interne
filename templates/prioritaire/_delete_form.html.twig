<form method="post" action="{{ path('prioritaire_delete', {'id': prioritaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prioritaire.id) }}">
    <button class="btn">Delete</button>
</form>
